# 쿠팡 중국 판매자 찾기 - 프로그램 로직 v2

## 전체 흐름

```
1. products.json 로드 (브랜드 + 검색 키워드)
2. seller_cache.json 로드 (이전에 확인한 판매자)
3. 각 품목마다:
   a. 쿠팡 검색 (심플한 키워드: "스투시 반팔", "나이키 에어포스 흰색")
   b. 검색 결과 1~30페이지 스캔 (DOM만, 네트워크 요청 없음)
   c. 배송일 긴 순서로 정렬 (16일+ 우선)
   d. 배송 5일 이내, 로켓직구/그로스, 판매자로켓(내일도착) → 절대 제외
   d-2. 해외배송(개인통관고유부호) → 절대 제외
   d-3. 노브랜드/개인 브랜드 상품 → 절대 제외 (글로벌 유명 브랜드만)
   e. 후보 중 배송 가장 긴 상품 1~2개 방문
   f. "다른 판매자 보기" 클릭 → 전체 판매자 + vendorItemId 추출 (**매번 열어서 전부 확인**)
   g. 캐시에 있는 판매자 → 재접속 없이 캐시 결과 사용
   g-2. 캐시에 **없는** 판매자 → 새 판매자! 판별 후 캐시 저장
   h. 최저가 50% 초과 판매자 → 제외
   i. 상호/대표자명으로 중국인 판별 → 캐시에 저장
      - "유한회사" 포함 = 91% 중국 (즉시 판별)
      - 유한회사 없어도 대표자가 중국식 이름이면 중국 (예: 유씨와이/옆루빈, 인레이션/옆무빈)
      - 주식회사여도 대표자 핑인이면 중국 (예: 리난가구/MENG JIANNAN)
      - 대표자 이름이 3글자가 아니거나, 3글자여도 한국 일반적 성씨가 아니면 중국인 간주
      - 한국 성씨: 김/이/박/최/정/강/조/윤/장/임/한/오/서/신/권/황/안/송/류/전/홍/고/문 등
      - 예: "옆루빈" = 3글자지만 "옆"은 한국 성씨 아님 → 중국인
      - 예: "김낙구" = 3글자 + "김" = 한국인
      - 영문이름이면 전부 핑인 체크 (YUAN, WEN, CHE 등도 중국)
   j. 구매 링크 생성: /vp/products/{pid}?vendorItemId={vid}
4. 교차 분석: 같은 판매자가 여러 품목에 등장하는 경우
   - "다른 판매자 보기"에서 캐시에 없는 새 판매자를 우선 탐색
   - 캐시에 이미 있는 판매자(한국인/중국인 모두) 건너뛰
   - 판매자 분산이 목적 (한 판매자에게 몰릴 위험 방지)
5. 결과 출력 (판매자, 대표자, 가격, **배송일**, 사이즈, 구매링크)
6. 결과 JSON 저장
```

## 검색 전략

### 키워드는 심플하게
- ✅ "스투시 반팔" / "나이키 에어포스 흰색"
- ❌ "스투시 반팔 티셔츠 남성 여성 공용" (너무 구체적)

### 여러 페이지 스캔
- 1페이지에서 배송 14일+ 상품이 있으면 바로 진행
- 없으면 2페이지, 3페이지까지 넘김
- 페이지 넘기는 사이 5초 대기

### 배송일 기준 (핵심 발견)
| 배송일 | 판매자 유형 | 조치 |
|--------|------------|------|
| 5일 이내 | 국내 판매자 확정 | **절대 제외** |
| 6~10일 | 국내 판매자 가능성 높음 | 스킵 (우선순위 낮음) |
| 10~14일 | 국내/해외 혼재 | 후보 (하지만 한국인인 경우 많음) |
| **16일+** | **중국 판매자 가능성 매우 높음** | **최우선 확인** |

→ 배송일 긴 순서(16일→14일→10일)로 정렬해서 확인

## 상품 페이지에서 추출하는 정보

### 판매자 정보
```js
// 판매자명
const seller = (txt.match(/판매자:\s*([^\n]{2,80})/)||[])[1]
  ?.trim().replace(/판매자\s*평가.*/,'').replace(/\s*다른 판매자.*/,'').trim();

// 대표자명 (판매자 정보 테이블)
const rm = txt.match(/상호\/?대표자\s*([^\n]{2,100})/);
let rep = null;
if (rm) {
  const parts = rm[1].split('/').map(s => s.trim());
  rep = parts.length >= 2 ? parts[parts.length - 1] : null;
}
```

### "다른 판매자 보기"에서 vendorItemId 추출
```js
// data-row-key = vendorItemId
const rows = document.querySelectorAll('tr[data-row-key]');
rows.forEach(r => {
  const vid = r.getAttribute('data-row-key');
  const text = r.textContent; // 가격 + 배송 + 판매자명 포함
  // 구매 링크: /vp/products/{pid}?vendorItemId={vid}
});
```

### ⚠️ "다른 판매자 보기" 전체 확인 필수
- **스크롤해서 판매자 전체를 확인해야 함** (7명인데 3명만 보는 실수 방지)
- `document.querySelectorAll('tr[data-row-key]')`로 전체 행을 가져오면 스크롤 없이도 전체 추출 가능
- 매번 전체 판매자 수를 콘솔에 출력해서 누락 방지

### 새 판매자 발견 로직
1. "다른 판매자 보기" 전체 목록 추출
2. 각 판매자명을 `seller_cache.json`과 대조
3. **캐시에 없는 판매자** → 관심 대상
4. "유한회사" 포함이면 → 즉시 중국 판매자로 판별 → 캐시 저장
5. 이 방식으로 같은 상품에서도 2번째, 3번째 중국 판매자 발견
6. **판매자 분산이 목적** — 한 판매자에게 몰릴 시 계정 정지 위험

### 사이즈
```js
// 신발: 숫자 3자리
// 의류: S/M/L/XL/2XL/XXL/FREE
document.querySelectorAll('li').forEach(li => {
  const v = li.textContent.trim();
  if (/^(\d{3}|S|M|L|XL|2XL|3XL|XXL|FREE)$/i.test(v)) sizes.push(v);
});
```

## 중국 판매자 판별

### 판매자명으로 판별
| 패턴 | 예시 | 판별 | 확률 |
|------|------|------|------|
| **"유한회사" 포함** | 루오엘 유한회사, 위드지아 유한회사 | **중국 법인** | **91% (33곳 중 30곳)** |
| "트레이딩/무역" 포함 | OO트레이딩 | 중국 수입상 | 유한회사와 겹침 |
| Co.,Ltd (주식회사 없이) | NOIRIC Co.,Ltd. | 중국식 영문 | |
| 중국어(한자) 포함 | 深圳科技 | 중국 | |
| **주식회사 + 핑인 대표자** | 리난가구 주식회사 / MENG JIANNAN | **중국** | 예외 케이스 |

> **핵심**: "유한회사"가 판매자명에 있으면 91% 확률로 중국 판매자. 대표자 확인 없이 바로 판별 가능.
> "주식회사"라도 대표자가 핑인(영문명)이면 중국 판매자 (리난가구/나우트레이드 사례).

### 대표자명으로 판별
| 패턴 | 예시 | 판별 |
|------|------|------|
| 핑인 영문 | LUO JIAJIA, ZHANG XIA, FENG YUJIA | 중국인 |
| 중국어 한자 | 张三 | 중국인 |
| 중국식 한글 음역 | 왕웨이, 류칭 | 중국인 |
| **한국식 이름** | **김낙구, 이화순, 정유진** | **한국인 → 제외** |
| **"주식회사"** | **주식회사 노이릭** | **한국 법인 → 제외** |

### 이름만으로 판별 안 되는 경우
- "춘향" 같은 이름 → 이름만으로 중국인인지 모름
- 이 경우 상품페이지의 판매자 정보에서 대표자명 직접 확인 필요
- 사용자가 직접 확인 후 cache에 수동 등록 가능

## 가격 필터
- 같은 상품의 다른 판매자 중 **최저가 대비 50% 초과는 제외**
- 계산: `maxPrice = lowestPrice * 1.5`
- 예: 최저가 85,440원 → 128,160원 초과 판매자 제외

## 판매자 캐시 (효율화)

### 원칙
- 한번 확인한 판매자는 **다시 접속하지 않음**
- seller_cache.json에 결과 저장
- 다음 실행 시 캐시된 결과 사용

### 접속하는 경우
- 캐시에 없는 새 판매자만

### 접속 안 하는 경우
- 캐시에 isChinese: false (한국인 확인됨)
- 캐시에 isChinese: true (중국인 확인됨 → 결과만 사용)
- 최저가 50% 초과 판매자
- 로켓직구/그로스 상품
- 배송 5일 이내 상품

## 추가 제외 조건

### 해외배송 상품 제외
- 상품 페이지에 "해외 배송 상품 안내", "개인통관고유부호" 표시 있으면 제외
- 이런 상품은 직접 해외에서 배송되는 것이라 로켓직구와 다름
- 확인 방법: `document.body.innerText.includes('개인통관고유부호')` 또는 `'해외 배송 상품'`

### 가격 하한선
- 기본: **5만원 미만 상품 제외**
- 예외: 모자/측/슬리퍼/액세서리 등 원래 저가인 품목은 **2만원부터** (예: 이미스 모자 29,570원, 크록스 슬리퍼 35,200원 포함)
- 저가 노브랜드/복제품 걸러내기 위함

## 봇차단 방지 (절대 위반 금지)

1. **fetch() API 절대 사용 금지**
2. **한번에 탭 1개만** - 병렬 열기 금지
3. **페이지 이동 사이 15~25초 랜덤 대기**
4. **팝업 탭 즉시 닫기**
5. **차단 감지 시 즉시 중단** ("사용권한이 없습니다")
6. **한 품목당 상품 페이지 최대 2개만 방문**

## 검색 결과 필터 (DOM만, 접속 없음)

```js
// 로켓직구/그로스 제외
const srcs = Array.from(el.querySelectorAll('img')).map(im => im.src || '');
const isRocket = srcs.some(s => s.includes('rocket_merchant') || s.includes('rocket_growth'));
const isGlobal = srcs.some(s => s.includes('global') || s.includes('coupang_global'));
if (isRocket || isGlobal) continue; // 제외

// 배송 5일 이내 제외
const dm = el.textContent.match(/(\d+)\/(\d+)/);
const days = Math.round((deliveryDate - today) / 86400000);
if (days <= 5) continue; // 절대 제외

// 배송일 긴 순 정렬 (16일+ 우선)
items.sort((a, b) => b.days - a.days);
```

## 실전 패턴 (확인된 사실)

### 신발 브랜드
- 배송 10~14일 상품에서 중국 판매자 발견됨
- "다른 판매자 보기"에 여러 중국 판매자가 있는 경우 많음
- 너우샤 유한회사 같은 판매자가 여러 브랜드에 걸쳐 등장

### 의류 브랜드 (스투시 등)
- 배송 6~14일은 한국 판매자인 경우가 많음
- **배송 16일+ 상품에서 중국 판매자 발견됨**
- 한 판매자(위드지아)가 브랜드 전체를 독점하는 경우 있음

### 판매자 스토어 페이지 활용
- 상품 페이지에서 "판매자 상품 보러가기" 링크 또는 `shop.coupang.com/vid/` URL
- 판매자 스토어에 들어가면 해당 판매자의 전체 상품군 한눈에 파악 가능
- 브랜드별 검색보다 효율적 (판매자 1명의 전체 상품 확인)

### 확인된 중국 판매자 목록 (33곳)
| 판매자 | 대표자 | 취급 브랜드 |
|--------|--------|------------|
| 루오엘 유한회사 | LUO JIAJIA | 나이키 신발 |
| 리프페이 유한회사 | ZHOU YEFEI | 아디다스 신발 |
| 라테야 유한회사 | HUANG YINGXIN | 뉴발란스 신발 |
| 너우샤 유한회사 | ZHANG XIA | 아식스, 살로몬 |
| 엘트록스 유한회사 | 미확인 | 아식스 |
| 에티베라 유한회사 | 미확인 | 살로몬 |
| 유한회사 체로아 | 미확인 | 살로몬 |
| 루비오 유한회사 | LU QING | 오니츠카타이거 |
| 춘향 | 미확인 | 오니츠카타이거 |
| 햇살마켓 유한회사 | TANG HAOXIANG | 휠라 |
| 위드지아 유한회사 | FENG YUJIA | 스투시 |
| 진쫐코너 유한회사 | ZHANG JINJUN | 아미, 폴로, 베이프 |
| 즈콠마켓 유한회사 | DONG ZHIQIAN | 노스페이스 패딩 |
| 리난가구 주식회사 | MENG JIANNAN | 데상트, 스투시 자켓 |
| 주식회사 나우트레이드 | SUN YEQING | 데상트 맨투맨 |
| 치항저우유한회사 | QI HANGZHOU | 데상트 바지, 노스 눅시 |
| 건이트레이딩 유한회사 | CHENG GUOJIAN | 데상트 신발 |
| 시카로스 유한회사 | LIU XIAOYE | 이미스, 게스 |
| 위즈무역 유한회사 | YU ZEXUE | 코치 가방 |
| 메이광펹 유한회사 | MEI GUANGPENG | 어그 슬리퍼 |
| 유한회사누볼라 | MO WENYU | 크록스 |
| 야오위팅 유한회사 | YAO YUTING | 버켄스탁 |
| 라에모이 유한회사 | GUO QIUYU | 오니츠카 |
| 유한회사 자웨이장 | ZHANG JIAWEI | 나이키 |
| 동칸링크 유한회사 | 미확인 | 아식스 |
| 레이린 유한회사 | 미확인 | 아식스 |
| 유한회사 페이텐롱트레이딩 | 미확인 | 아식스 |
| 드로이아 유한회사 | 미확인 | 나이키 |
| 커튁비즈유한회사 | 미확인 | 오니츠카 |
| 캁젤 유한회사 | 미확인 | 오니츠카 |
| 유한회사 아크온 | 미확인 | 노스페이스 |
| 시에시즌 유한회사 | 미확인 | 데상트 |
| 구이평 유한회사 | 미확인 | 노스페이스 |

## 모니터링 로직 (monitor.js)

중국 판매자는 계정정지 당하고 새 이름으로 다시 등장한다.
따라서 주기적으로 새 판매자를 탐지해야 한다.

```
1. products.json 품목 순회 (랜덤 순서)
2. 각 품목 검색 → 배송 10일+ 상품 방문
3. "다른 판매자 보기" → 전체 판매자 추출
4. 캐시에 없는 판매자 = 새 판매자
5. vendorItemId로 해당 판매자 페이지 접속
6. 대표자명 확인 → 중국인 판별
7. 결과 캐시 저장 + 알림
```

### 판별 기준 (대표자명 기반)
- 영문 이름: 한국 영문성씨(KIM/LEE/PARK 등) 아니면 중국인
- 한글 이름: 3글자 + 한국 성씨(김/이/박 등) 아니면 중국인 간주
- 한자 포함: 중국인
- 유한회사 없어도 대표자가 중국인이면 중국 판매자

### 실행 주기
- 매일 또는 매주 실행 권장
- 캐시 덕분에 이미 확인한 판매자는 재접속 없음
- 새 판매자만 탐지하므로 실행 시간 점점 단축
- 랜덤 순서로 검색하여 봇 패턴 방지
